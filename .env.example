# KubeSentry only analyzes supplied local manifest text or bundled synthetic fixtures.
# It does not require Kubernetes credentials, cluster endpoints, or external API keys.
KUBESENTRY_API_PORT=4900
KUBESENTRY_WEB_PORT=5200
